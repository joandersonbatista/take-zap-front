/* import { AppRoute } from '@/navigation/app-route'; */
import { RoutesManager } from '@/navigation/routes-manager';

/* import { HELP_SCREENS } from '@/modules/help/navigation/screen-definitions';
import { HelpContainer } from '@/modules/help/screens/';

import { MenuItemsManager } from '@/menu-items-manager'; */

export const registerScreens = (routesManager: typeof RoutesManager): void => {
  /* const appRoute = new AppRoute(HelpContainer, {
    isPrivate: true,
    path: HELP_SCREENS.help,
  });

  const menuItem = {
    label: 'help.menu_label',
    icon: 'help',
    order: 8,
    screenName: HELP_SCREENS.help,
    onBottom: true,
  };

  MenuItemsManager.addMenu([menuItem]);

  routesManager.addSection(appRoute); */
};
