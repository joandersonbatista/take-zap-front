/* import { AppRoute } from '@/navigation/app-route'; */
import { RoutesManager } from '@/navigation/routes-manager';

/* import { DASHBOARD_SCREENS } from '@/modules/dashboard/navigation/screen-definitions';
import { DashboardContainer } from '@/modules/dashboard/screens/';

import { MenuItemsManager } from '@/menu-items-manager'; */

export const registerScreens = (routesManager: typeof RoutesManager): void => {
  /* const appRoute = new AppRoute(DashboardContainer, {
    isPrivate: true,
    path: DASHBOARD_SCREENS.dashboard,
  });

  const menuItem = {
    label: 'dashboard.menu_label',
    icon: 'dashboard',
    order: 1,
    screenName: DASHBOARD_SCREENS.dashboard,
  };

  MenuItemsManager.addMenu([menuItem]);

  routesManager.addSection(appRoute); */
};
